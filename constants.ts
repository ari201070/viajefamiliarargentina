export interface City {
  id: string;
  name: string;
  // Add other properties as needed
}

export const CITIES: City[] = [
  // Example city data will be added later
  // { id: 'buenosaires', name: 'Buenos Aires' },
  // { id: 'rosario', name: 'Rosario' },
];
